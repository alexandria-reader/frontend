describe('renders the home page', () => {
  it("renders correctly", () => {
    cy.visit("/")
  })
})

describe ('can log in an existing user', () => {
  it('logs in user', () => {
    // cy.get('')
  })
})